import type { SwitchProps } from 'antd';
import { Switch } from 'antd';
import type { FC } from 'react';

export const FormSwitch: FC<SwitchProps> = (props) => {
  const {  } = props;
  return <Switch  {...props} />;
};
